-- La funzione addizione prende in input una coppia di interi e restituisce la loro somma.
-- Una coppia/tupla è un tipo di dato che contiene due valori di tipo uguale o diverso.

addizione :: (Int, Int) -> Int
addizione (x, y) = x + y